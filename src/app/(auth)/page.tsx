export default function AuthenticationPage() {
 return (
    // AuthForm components that has input and error handling.
    // AuthButtons which enables authentication with github and google.
    // state toggling between register and login.
 )
}